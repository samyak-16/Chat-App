import { useEffect, useState } from 'react';

const ChatPage = () => {
  const [chats, setChats] = useState([]); //We will get the sorted chat "-1" from the backend directly so no need to sort 
  const [SelectedChat, setSelectedChat] = useState(null);
};
